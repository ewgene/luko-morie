

    if(document.getElementById('bg_01')) {          
      const lr_01Left = computed(() => {
        return lr_01.value?.$el.offsetLeft})
      const lr_01Right = computed(() => {
        return lr_01.value?.$el.offsetRight })
    }
    if(document.getElementById('bg_02')) {
      const lr_02Left = computed(() => {
        return lr_02.value?.$el.offsetLeft})
      const lr_02Right = computed(() => {
        return lr_02.value?.$el.offsetRight })
    }
    if(document.getElementById('bg_03')) {
      const lr_03Left = computed(() => {
        return lr_03.value?.$el.offsetLeft})
      const lr_03Right = computed(() => {
        return lr_03.value?.$el.offsetRight })
    }
    if(document.getElementById('bg_04')) {
      const lr_04Left = computed(() => {
        return lr_04.value?.$el.offsetLeft})
      const lr_04Right = computed(() => {
        return lr_04.value?.$el.offsetRight })
    }
    if(document.getElementById('bg_05')) {
      const lr_05Left = computed(() => {
        return lr_05.value?.$el.offsetLeft})
      const lr_05Right = computed(() => {
        return lr_05.value?.$el.offsetRight })
    }